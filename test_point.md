# 8세 아동 맞춤형 영어 단어 보상 애플리케이션 'Minty Stamp' 구현 명세서

## 1. Project Overview

### 1.1 프로젝트 명칭 및 개요
본 프로젝트의 공식 명칭은 'Minty Stamp(민티 스탬프: 10개의 별을 모으는 영어 단어 모험)'이며, 초등학교 1학년(만 8세) 아동의 기초 어휘 학습 지속성을 견인하기 위해 기획된 단독 데스크톱 구동형 파이썬 웹 GUI 애플리케이션이다. 주 2회 치러지는 정기 영어 단어 시험에서 100점 만점을 획득할 때마다 시각화된 디지털 칭찬 도장판에 스탬프를 적립하고, 누적 10개의 도장이 채워지는 시점에 사전에 부모와 합의하여 등록한 위시리스트 보상을 지급하는 외재적 동기부여 메커니즘을 핵심 기능으로 삼는다.

### 1.2 기획 배경 및 행동 경제학적 분석
아동 발달 심리학에 따르면 만 8세 전후의 아동은 장기적이고 추상적인 미래 가치보다 단기적이고 시각적으로 체감 가능한 즉각적 피드백에 민감하게 반응한다. 행동 변화를 유도하는 포그 행동 모델(Fogg Behavior Model) 관점에서도 반복 학습 행동이 정착되려면 동기(Motivation), 수행 역량(Ability), 그리고 즉각적인 방아쇠(Trigger)의 결합이 필수적이다. 전통적인 종이 도장판은 분실 위험이 높고 아동의 능동적인 상호작용을 유도하는 데 한계가 있으며, 상업용 학습 플랫폼은 과도한 과금 요소나 시각적 혼란을 초래한다. 이에 따라 부모와 아동이 한 화면을 공유하며 단어 시험 결과를 승인하고, 아동 스스로 원하는 선물을 등록·선택하는 자율성을 보장하는 협력형 보상 시스템이 요구된다.

| 시스템 항목 | 세부 정의 내용 |
| :--- | :--- |
| **소프트웨어 명칭** | Minty Stamp (로컬 실행형 인터랙티브 스탬프 시스템) |
| **주요 사용자 계층** | 8세 여아(학습자/피보상자) 및 학부모(시험 검증자/관리자) 공동 운영 |
| **동기부여 트리거** | 주 2회 영어 단어 시험 100점 달성 시 1 스탬프 지급 (목표치: 10 스탬프) |
| **핵심 보상 규칙** | 10 스탬프 완주 시 사전 등록된 위시리스트 중 최종 보상 1종 해금 및 선택 |
| **운영 환경** | 로컬 PC 독립 구동 환경 (Python 3.10+, VS Code 및 로컬 브라우저) |

### 1.3 시스템 핵심 지향점
본 시스템은 정량적 목표 시각화, 아동 주도적 자율성, 데이터 신뢰성이라는 세 가지 아키텍처 원칙을 충족하도록 설계된다. 10칸으로 구성된 칭찬 도장판은 남은 여정을 직관적으로 파악할 수 있게 하여 성취 동기를 자극하며, 위시리스트의 직접 관리는 심리적 오너십을 형성한다. 또한 네트워크 단절이나 시스템 재부팅 후에도 점수와 이력이 손실되지 않는 견고한 로컬 파일 지속성을 확보하고, 감각적 도파민 반응을 촉진하는 다감각 피드백 요소를 도입한다.

---

## 2. System Architecture & Directory Structure

### 2.1 기술 스택 선정 및 아키텍처 분석
초보자와 아동이 공동으로 사용하는 환경에서 빠른 개발 반복 주기와 풍부한 시각 효과를 양립하기 위해 Streamlit 프레임워크가 단독 GUI 솔루션으로 채택되었다. 데스크톱 네이티브 프레임워크인 PyQt 및 CustomTkinter는 윈도우 스레딩 및 이벤트 루프 설계에 높은 코드 복잡도를 요구하며, 웹 수준의 유려한 CSS 스타일링이나 파티클 애니메이션을 구현하는 데 상당한 개발 오버헤드가 발생한다. 반면 Streamlit은 로컬 브라우저 렌더링 엔진을 활용하므로 파스텔 톤의 커스텀 웹 폰트 적용, HTML5 캔버스 기반 Confetti 효과, Base64 오디오 자동 재생 기술을 효율적으로 통합할 수 있다.

| 평가 축 | Streamlit (최종 선정) | CustomTkinter / PyQt |
| :--- | :--- | :--- |
| **프론트엔드 표현력** | HTML/CSS 주입을 통한 파스텔 민트 디자인 및 반응형 카드 레이아웃 극대화 | Tkinter 캔버스 기반 렌더링으로 둥근 테두리 및 그림자 효과 구현 난도 높음 |
| **시각/청각 인터랙션** | `canvas-confetti` JS 라이브러리 및 Base64 인코딩 오디오 재생 즉시 연동 | 별도 게임 엔진(`pygame`) 또는 OS 종속적 미디어 라이브러리 결합 필요 |
| **상태 관리 및 생산성** | 선언적 `st.session_state` 모델을 통한 신속한 프로토타이핑 및 데이터 동기화 | 시그널-슬롯 매핑 및 보일러플레이트 코드로 인한 유지보수 복잡도 증가 |
| **코딩 어시스턴트 협업** | 간결한 파이썬 스크립트 구조로 Gemini Code Assistant의 코드 생성 정확도 극대화 | 위젯 계층 구조 및 좌표 배치 코드로 인한 AI 생성 오차율 증가 |

### 2.2 디렉터리 및 파일 구조
관심사 분리(Separation of Concerns) 원칙에 입각하여 시스템 데이터 계층, 정적 스타일 및 사운드 애셋, 비즈니스 로직, 프론트엔드 뷰를 엄격히 분리한다. 시스템 파일 배치는 다음과 같은 표준 계층 구조를 갖는다.

* **`minty_stamp/`** : 프로젝트 루트 디렉터리
  * **`.streamlit/`** : 프레임워크 환경 설정 디렉터리
    * `config.toml` : 테마 색상 기본값, 서버 포트 및 브라우저 실행 옵션 정의
  * **`assets/`** : 정적 미디어 및 디자인 리소스 저장소
    * **`css/`** : 커스텀 스타일시트 디렉터리
      * `style.css` : 파스텔 민트 테마, 둥근 카드, 웹 폰트 정의 CSS
    * **`sounds/`** : 효과음 오디오 파일 디렉터리
      * `stamp.mp3` : 스탬프 획득 시 재생되는 실로폰 효과음
      * `celebration.mp3` : 10개 완주 시 재생되는 팡파레 효과음
  * **`data/`** : 애플리케이션 영구 저장소
    * `app_data.json` : 프로필, 위시리스트, 획득 이력이 통합 저장되는 단일 JSON 파일
  * **`utils/`** : 핵심 비즈니스 로직 및 헬퍼 모듈 패키지
    * `__init__.py` : 패키지 초기화 파일
    * `storage.py` : JSON 파일 I/O, 스탬프 적립/롤백, 원자적 데이터 쓰기 엔진
    * `effects.py` : 커스텀 CSS 주입, 파티클 효과, Base64 오디오 재생 모듈
  * `app.py` : 메인 사용자 인터페이스, 탭 렌더링 및 세션 컨트롤러
  * `requirements.txt` : 프로젝트 의존성 라이브러리 목록 명세
  * `spec.md` : 시스템 소프트웨어 요구 명세서

### 2.3 데이터 저장 스키마 (Data Persistence Schema)
데이터 영구 보존을 위해 SQLite와 같은 관계형 데이터베이스 대신 이식성과 백업 편의성이 우수한 단일 로컬 JSON 구조를 채택한다. 모든 데이터는 UTF-8 형식으로 인코딩되며, 시스템 시작 시 파일 무결성을 검증하고 없을 경우 자동 복원한다.

```json
{
  "profile": {
    "child_name": "예원",
    "target_stamps": 10,
    "current_stamps": 3,
    "daily_memo": "오늘도 단어 10개 완벽하게 외우기 화이팅! ⭐",
    "updated_at": "2025-05-14T18:30:00"
  },
  "wishlist": [
    {
      "id": "wish-001",
      "title": "산리오 시나모롤 인형 필통",
      "image_url": "https://example.com/images/cinnamoroll.png",
      "memo": "100점 10개 받으면 아빠가 사주기로 한 선물",
      "is_redeemed": false,
      "created_at": "2025-05-01T10:00:00"
    },
    {
      "id": "wish-002",
      "title": "레고 디즈니 공주 캐슬",
      "image_url": "https://example.com/images/lego.png",
      "memo": "동생이랑 같이 조립할 장난감",
      "is_redeemed": false,
      "created_at": "2025-05-05T14:20:00"
    }
  ],
  "history": [
    {
      "id": "hist-001",
      "date": "2025-05-07",
      "score": 100,
      "word_count": 20,
      "note": "Day 1 단어 시험 만점 달성!",
      "stamps_after": 1,
      "timestamp": "2025-05-07T19:10:00"
    },
    {
      "id": "hist-002",
      "date": "2025-05-10",
      "score": 100,
      "word_count": 20,
      "note": "Day 2 불규칙 동사 철자 완벽!",
      "stamps_after": 2,
      "timestamp": "2025-05-10T18:45:00"
    },
    {
      "id": "hist-003",
      "date": "2025-05-14",
      "score": 100,
      "word_count": 25,
      "note": "Day 3 교과서 핵심 표현 만점!",
      "stamps_after": 3,
      "timestamp": "2025-05-14T18:30:00"
    }
  ]
}
```

---

## 3. UI / UX Design Specs

### 3.1 8세 아동 특화 시각 및 상호작용 디자인 원칙
아동용 인터페이스 디자인은 인지 부하를 최소화하고 조작 직관성을 극대화해야 한다. 텍스트 위주의 정보 표현을 배제하고 크고 명확한 시각적 앵커(별, 도장, 트로피)를 배치하여 읽기 능력이 발달 중인 아동도 직관적으로 상태를 인지할 수 있도록 유도한다. 모든 버튼과 인터랙션 영역은 48픽셀 이상의 넉넉한 터치 영역(Tappable area)을 확보하고, 모서리는 부드러운 곡선(16px 이상의 `border-radius`)으로 마감하여 심리적 안정감과 친근감을 부여한다.

### 3.2 컬러 팔레트 및 타이포그래피 사양
주요 색상은 밝고 경쾌한 파스텔 민트 톤을 기저에 두고, 성취의 상징인 골드 옐로우와 경고성 액션을 나타내는 소프트 코랄을 보조 색상으로 조화롭게 배분한다.

| 역할 (Token Name) | 색상명 (Color Name) | Hex Code | UI 적용 컴포넌트 및 용도 |
| :--- | :--- | :--- | :--- |
| **Primary** | Fresh Mint | `#5CE1E6` | 주요 강조 버튼, 스탬프 슬롯 활성 테두리, 상단 배너 액센트 |
| **Primary Light** | Soft Cream Mint | `#DFF6F5` | 메인 컨테이너 박스, 위시리스트 카드 배경, 획득 도장 바탕 |
| **Secondary** | Peppermint Green | `#70D6BC` | 탭 활성화 인디케이터, 진행률 게이지 바 전경색 |
| **Accent Gold** | Star Yellow | `#FFD166` | 100점 스탬프 중앙 별 아이콘, 10점 피날레 왕관 이모지 |
| **Accent Coral** | Sweet Coral | `#FF6B6B` | 실수 취소 버튼, 삭제 확인 뱃지, 축하 리본 배경 |
| **Background** | Pale Mint Mist | `#F7FCFB` | 최외곽 웹페이지 기본 배경색 |
| **Text Main** | Dark Slate Gray | `#2C3E50` | 본문 가독성을 보장하는 고대비 텍스트 색상 (명도비 준수) |
| **Text Muted** | Soft Teal Gray | `#7F9C96` | 날짜 표기, 부가 설명 라벨, 비활성 슬롯 안내 텍스트 |

타이포그래피는 상업용 고딕 계열의 경직성을 탈피하고 친근한 필기체 질감을 부여하기 위해 구글 웹폰트 'Jua(주아체)'와 'Gaegu(개구쟁이체)'를 웹폰트로 동적 임포트하여 전역 렌더링에 적용한다.

### 3.3 화면 레이아웃 및 뷰 계층

#### 3.3.1 상단 글로벌 영역 (Header & Motivation Area)
화면 최상단에는 아동의 이름과 함께 "예원이의 매일매일 영어 단어 100점 도장판" 타이틀이 대형 배너 형태로 출력된다. 타이틀 하단에는 아동이 직접 키보드로 입력하고 갱신할 수 있는 '오늘의 각오 한마디' 말풍선 위젯이 자리 잡는다. 그 바로 밑에는 목표치인 10개 도장 중 현재 몇 개를 달성했는지를 실시간으로 나타내는 프로그레스 바가 민트-골드 그라데이션 형태로 가로지르며, "현재 10개 중 3개 달성! 30% 완료!"와 같은 직관적인 안내 문구가 병기된다.

#### 3.3.2 메인 탭 1: 칭찬 도장판 (10-Slot Stamp Board View)
애플리케이션의 핵심 영역으로, 2행 5열(2x5 Grid)의 넉넉한 간격을 가진 10개의 둥근 사각 카드가 배치된다.
- **1번부터 9번 슬롯**: 미획득 상태에서는 점선 테두리(`border: 2px dashed #B2DFDB`)와 연회색 슬롯 번호가 표시되며, 도장 획득 시 부드러운 확대 애니메이션과 함께 황금빛 별 도장(`🌟`), 획득 날짜 라벨(예: `5/14`), "참 잘했어요!" 리본 텍스트가 표시된다.
- **10번째 피날레 슬롯**: 특별한 목표 지점임을 한눈에 알 수 있도록 황금빛 왕관(`👑`) 일러스트와 펄스(Pulse) 테두리 애니메이션이 기본 적용되어 아동의 최종 목표 도달 심리를 강력하게 자극한다.

#### 3.3.3 메인 탭 2: 선물 위시리스트 (Wishlist Registry & Gallery)
아동과 부모가 함께 원하는 보상을 정의하는 공간이다. 상단에는 '새로운 선물 후보 등록' 접이식 폼(`st.expander`)이 제공되어 선물 이름, 웹 이미지 URL, 보상 선정 이유 메모를 입력할 수 있다. 하단 갤러리에는 등록된 선물들이 3열 반응형 그리드 카드로 렌더링된다. 스탬프가 10개 미만일 때는 '아직 도장을 모으는 중이에요 (3/10)' 상태 배너가 표시되며 선택 버튼이 비활성화되지만, 10개가 채워지면 '🎉 최종 선물로 선택하기!' 버튼이 밝은 민트색으로 활성화되어 아동이 스스로 보상을 확정할 수 있다.

#### 3.3.4 메인 탭 3: 성공 일기 & 타임라인 (History Timeline)
과거의 성취를 복기하여 자기효능감을 증진할 수 있도록 날짜별 단어 시험 이력을 역순 피드 형태로 시각화한다. 각 기록 카드에는 시험 시행 일자, 시험 점수(100점 만점 뱃지), 암기 단어 수, 아동의 학습 메모가 일목요연하게 표시된다. 상단에는 '총 누적 100점 횟수', '누적 암기 단어 수'를 요약하는 대형 메트릭 위젯이 배치된다.

#### 3.3.5 사이드바: 학부모 관리 패널 (Parental Supervisor Panel)
아동의 실수나 부정 조작을 방지하기 위해 시험 승인 및 데이터 조정 기능은 사이드바에 집약된다.
- **부모님 확인 모드 토글**: 잠금 해제 스위치를 활성화해야 조작 버튼이 노출된다.
- **100점 도장 쾅! 찍기 버튼**: 시험 일자, 단어 개수, 부모의 칭찬 한마디를 입력하고 승인 버튼을 누르면 스탬프가 1 증가한다.
- **실수 취소 (Rollback) 버튼**: 잘못된 클릭으로 도장이 적립된 경우 마지막 1개를 회수하고 이전 상태로 되돌린다.
- **새로운 10개 도전 시작 (Cycle Reset) 버튼**: 10개 도장을 모두 채우고 선물을 수령한 후, 기존 히스토리는 보존한 채 도장판 슬롯만 0으로 리셋한다.

### 3.4 인터랙션 및 감각 피드백 명세
스탬프 적립 승인이 발생하는 순간, 화면 좌우 양측에서 `canvas-confetti` 엔진을 통해 다채로운 색상의 파티클이 일제히 분사된다. 동시에 HTML5 Audio 컴포넌트를 매개로 경쾌한 챠임 사운드가 재생되어 청각적 만족감을 제공한다. 

누적 도장이 10개에 도달하는 피날레 상황에서는 일반 효과보다 3배 긴 5초 동안 전면 화면을 뒤덮는 슈퍼 폭죽 애니메이션이 실행되며, 승리의 팡파레 오디오가 울려 퍼진다. 화면 중앙에는 "와아! 10개의 별을 모두 모았어요! 약속한 선물을 골라볼까요?" 문구가 포함된 대형 축하 모달 다이얼로그가 자동 팝업되어 위시리스트 탭으로 자연스럽게 전환을 유도한다.

---

## 4. Functional Requirements (Features)

### 4.1 F-01: 스탬프 적립 및 차감 관리

| 식별자 | 기능 세부 명세 | 검증 기준 및 비즈니스 규칙 |
| :--- | :--- | :--- |
| **FR-01-1** | **100점 스탬프 신규 적립** | `current_stamps`가 10 미만일 때만 수행 가능. 실행 시 스탬프 카운트 1 증가 및 즉시 파일 저장. |
| **FR-01-2** | **시험 로그 자동 동시 기록** | 스탬프 증가와 동시에 당일 날짜, 단어 수, 칭찬 메모를 포함하는 객체를 `history` 배열 최상단에 삽입. |
| **FR-01-3** | **실수 취소 (Undo Rollback)** | `current_stamps`가 1 이상일 때 동작. 스탬프 카운트를 1 감소시키고 가장 최근 생성된 히스토리 로그 1건 제거. |
| **FR-01-4** | **보상 지급 후 사이클 리셋** | 10개 도달 후 최종 보상이 확정되면, 기존 누적 히스토리는 유지한 채 `current_stamps`를 0으로 초기화. |

### 4.2 F-02: 선물 위시리스트 관리 (CRUD & Selection)

| 식별자 | 기능 세부 명세 | 검증 기준 및 비즈니스 규칙 |
| :--- | :--- | :--- |
| **FR-02-1** | **신규 선물 후보 등록** | 선물 이름(필수), 이미지 URL(선택), 메모(선택)를 입력받아 고유 UUID를 부여한 후 `wishlist` 배열에 추가. |
| **FR-02-2** | **이미지 예외 처리 대체** | 이미지 URL이 누락되었거나 로드 실패 시, 사전 준비된 귀여운 선물 상자 기본 플레이스홀더 이미지로 대체. |
| **FR-02-3** | **위시리스트 항목 삭제** | 등록된 선물 카드 우측 상단의 삭제 버튼 클릭 시 배열에서 즉시 필터링 후 영구 저장소에 동기화. |
| **FR-02-4** | **10점 피날레 보상 확정** | `current_stamps == 10` 도달 시 활성화되는 선택 버튼을 누르면 해당 항목의 `is_redeemed`를 `true`로 갱신. |

### 4.3 F-03: 데이터 영구 저장 및 무결성 관리

| 식별자 | 기능 세부 명세 | 검증 기준 및 비즈니스 규칙 |
| :--- | :--- | :--- |
| **FR-03-1** | **시작 시 자동 로드 및 초기화** | 앱 실행 시 `data/app_data.json` 파일을 탐색하며, 파일 부재 시 기본 스키마 템플릿을 생성하고 세션에 로드. |
| **FR-03-2** | **원자적 디스크 쓰기 (Atomic Write)** | 데이터 저장 시 임시 파일(`.tmp`)에 먼저 기록한 후 원자적 이름 변경(`os.replace`)을 적용하여 파일 손상 원천 차단. |
| **FR-03-3** | **한글 인코딩 보존** | JSON 직렬화 시 `ensure_ascii=False`, `indent=2` 옵션을 강제하여 한글 깨짐을 방지하고 가독성 유지. |

### 4.4 F-04: 부가 기능 및 게이미피케이션

| 식별자 | 기능 세부 명세 | 검증 기준 및 비즈니스 규칙 |
| :--- | :--- | :--- |
| **FR-04-1** | **오늘의 각오 한마디 편집** | 아동이 텍스트 필드에 직접 각오를 작성하고 저장 버튼을 누르면 `profile.daily_memo`가 업데이트되고 상단 배너에 반영. |
| **FR-04-2** | **목표 달성 게이지 실시간 연동** | `(current_stamps / 10) * 100` 비율을 `st.progress` 바에 투영하고 직관적인 잔여 도장 수 계산 문구 노출. |
| **FR-04-3** | **누적 성공 일기 렌더링** | `history` 배열의 데이터를 파싱하여 최근 시험 순서대로 카드 타임라인 피드를 생성하고 총 암기 단어 수 합산 표시. |

---

## 5. Component & Logic Detailed Design

### 5.1 아키텍처 생명주기 및 상호작용 흐름
사용자가 로컬 브라우저를 통해 `app.py`에 접근하면 Streamlit의 스크립트 실행 루프가 시작된다. 앱 시작 즉시 `storage.load_data()`가 호출되어 디스크의 `app_data.json` 내용을 메모리 세션 상태(`st.session_state['app_data']`)로 가져온다. 이후 `effects.inject_custom_css()`를 통해 스타일시트가 주입되어 브라우저 렌더링 트리를 장식한다.

부모가 사이드바에서 스탬프 적립 버튼을 클릭하면 `storage.add_stamp_record()`가 호출되어 데이터 딕셔너리가 갱신되고, 변경 사항이 즉시 `storage.save_data()`를 통해 JSON 파일로 동기화된다. 쓰기 작업이 성공하면 `effects.trigger_confetti()`와 `effects.play_sound('stamp')`가 호출되어 화면에 파티클과 효과음을 생성한 뒤, `st.rerun()`을 통해 전체 뷰를 갱신하여 최신 스탬프 상태(1 증가)를 화면에 반영한다.

### 5.2 모듈별 함수 상세 명세

#### 5.2.1 `utils/storage.py` (영구 데이터 입출력 엔진)

| 함수 시그니처 | 입출력 파라미터 및 반환형 | 내부 알고리즘 및 예외 처리 정책 |
| :--- | :--- | :--- |
| `get_data_filepath() -> Path` | **입력**: 없음<br>**출력**: `pathlib.Path` | 애플리케이션 루트 기준 `data/` 디렉터리 존재 여부를 확인하고 없으면 `mkdir(parents=True)` 실행 후 `data/app_data.json` 경로 객체 반환. |
| `get_initial_schema() -> dict` | **입력**: 없음<br>**출력**: 기본 구조 `dict` | 스탬프 0개, 기본 프로필, 빈 위시리스트 및 히스토리 리스트를 포함하는 표준 딕셔너리 템플릿 반환. |
| `load_data() -> dict` | **입력**: 없음<br>**출력**: 애플리케이션 데이터 `dict` | 파일 존재 시 `json.load()`로 역직렬화. 파일 미존재 시 `get_initial_schema()`를 디스크에 쓰고 반환. JSON 깨짐 발생 시 백업 파일 로드 또는 초기 스키마 반환. |
| `save_data(data: dict) -> bool` | **입력**: `data` (`dict`)<br>**출력**: 성공 여부 `bool` | 임시 파일(`app_data.json.tmp`)에 `utf-8` 인코딩 및 `ensure_ascii=False`로 기록 후 `os.replace`로 원자적 교체. I/O 에러 발생 시 콘솔 로깅 후 `False` 반환. |
| `add_stamp_record(score: int, word_count: int, note: str) -> tuple[bool, str]` | **입력**: 시험 점수, 단어 수, 메모<br>**출력**: `(성공여부, 메시지)` | `current_stamps >= 10`인 경우 실패 반환. 10 미만인 경우 카운트 1 증가, 고유 ID(`hist-UUID`)를 가진 기록 객체를 `history` 앞단에 추가한 뒤 `save_data` 호출. |
| `undo_last_stamp() -> tuple[bool, str]` | **입력**: 없음<br>**출력**: `(성공여부, 메시지)` | `current_stamps <= 0`인 경우 실패 반환. 카운트 1 감소, `history` 리스트에서 가장 최근 항목(Index 0)을 팝(Pop)하여 제거한 뒤 `save_data` 호출. |
| `add_wishlist_item(title: str, image_url: str, memo: str) -> bool` | **입력**: 선물명, 이미지 URL, 메모<br>**출력**: 성공 여부 `bool` | 필수값(`title`) 유효성 검증 후 고유 ID(`wish-UUID`)를 부여하여 `wishlist` 배열에 추가하고 파일 동기화. |
| `delete_wishlist_item(item_id: str) -> bool` | **입력**: 선물 항목 고유 `id`<br>**출력**: 성공 여부 `bool` | 해당 ID와 일치하는 요소를 `wishlist` 리스트에서 배제(Filter)한 후 파일 동기화. |
| `redeem_reward(item_id: str) -> bool` | **입력**: 선물 항목 고유 `id`<br>**출력**: 성공 여부 `bool` | 대상 항목의 `is_redeemed` 플래그를 `true`로 수정하고 타임스탬프를 갱신한 뒤 파일 저장. |
| `reset_cycle() -> bool` | **입력**: 없음<br>**출력**: 성공 여부 `bool` | `current_stamps`를 0으로 초기화하고 히스토리는 그대로 보존한 채 `save_data` 호출. |

#### 5.2.2 `utils/effects.py` (시각 및 청각 인터랙션 주입 모듈)

| 함수 시그니처 | 입출력 파라미터 및 반환형 | 내부 알고리즘 및 렌더링 메커니즘 |
| :--- | :--- | :--- |
| `inject_custom_css() -> None` | **입력**: 없음<br>**출력**: 없음 | `assets/css/style.css` 파일을 UTF-8로 읽어온 후 `st.markdown(f"<style>{css}</style>", unsafe_allow_html=True)` 구문으로 웹 뷰 헤더에 주입. |
| `trigger_confetti(mode: str = "normal") -> None` | **입력**: `mode` ('normal' 또는 'grand')<br>**출력**: 없음 | CDN 경로의 `canvas-confetti` 라이브러리를 로드하는 인라인 스크립트를 생성하여 `st.components.v1.html`로 실행. 'grand' 모드 시 발사각과 파티클 수를 3배 증폭. |
| `play_sound(sound_key: str) -> None` | **입력**: `sound_key` ('stamp' 또는 'celebration')<br>**출력**: 없음 | 대응하는 `assets/sounds/*.mp3` 파일을 바이너리로 읽어 Base64 문자열로 변환. 이를 `<audio autoplay style="display:none">` 태그 형태로 조립하여 비가시적 렌더링. |

#### 5.2.3 `app.py` (뷰 컨트롤러 및 컴포넌트 렌더러)

| 함수/컴포넌트 명칭 | 담당 책임 및 렌더링 인터페이스 명세 |
| :--- | :--- |
| `main()` | 전체 앱의 진입점. 세션 상태 초기화, CSS 주입, 상단 헤더, 메인 탭 3종 및 부모 패널 렌더링 분기 제어. |
| `render_header(data: dict)` | 타이틀 배너, 오늘의 각오 입력 폼, `st.progress` 게이지 바를 상단에 배치하고 메모 수정 시 세션과 스토리지 동기화. |
| `render_stamp_board(data: dict)` | 2행 5열 그리드를 생성하여 1번부터 10번 슬롯 카드 출력. 획득 여부에 따라 별 도장 카드 또는 점선 대기 슬롯 카드 조건부 렌더링. |
| `render_wishlist_view(data: dict)` | 신규 선물 등록용 `st.expander` 폼 및 3열 선물 카드 그리드 배치. 10개 달성 여부에 따라 최종 수령 확정 버튼 활성화 제어. |
| `render_history_view(data: dict)` | 누적 통계 메트릭(총 100점 횟수, 총 단어 수) 카드 출력 및 시간 역순 시험 이력 피드 렌더링. |
| `render_parent_sidebar(data: dict)` | 부모 모드 인증 토글, 100점 도장 적립 폼, 실수 취소 버튼, 10개 완주 후 초기화 버튼 제공 및 스토리지 연동. |

---

## 6. Gemini Code Assistant Implementation Prompts Guide

VS Code 환경에서 Gemini Code Assistant를 활용하여 본 시스템을 결함 없이 단계적으로 구현할 수 있는 4단계 순차 프롬프트 엔지니어링 가이드라인이다.

### Step 1: 데이터 모델 및 로컬 영구 스토리지 엔진 구현 (`utils/storage.py`)

당신은 시니어 파이썬 백엔드 아키텍트입니다.
초등학교 1학년(8세) 아이의 영어 단어 100점 보상 앱인 'Minty Stamp'의 로컬 데이터 지속성 모듈 `utils/storage.py`를 구현해야 합니다.

아래 요구사항을 완벽히 충족하는 파이썬 코드를 작성해주세요:
1. `pathlib.Path`를 사용하여 애플리케이션 루트 기준 `data/app_data.json`의 경로를 안정적으로 다루는 함수를 작성하세요.
2. `app_data.json` 파일이 없으면 자동으로 기본 JSON 구조(profile: child_name, target_stamps=10, current_stamps=0, daily_memo; wishlist: 빈 리스트; history: 빈 리스트)를 생성하고 반환하는 `load_data()` 함수를 작성하세요.
3. 데이터 저장 시 중간 쓰기 오류로 파일이 손상되는 것을 방지하기 위해, 임시 파일(`app_data.json.tmp`)에 먼저 쓰고 `os.replace`로 교체하는 원자적 저장 함수 `save_data(data: dict) -> bool`을 구현하세요. 한글 인코딩 깨짐을 방지하기 위해 `ensure_ascii=False, indent=2`를 필수 적용해야 합니다.
4. 스탬프 적립 및 취소 비즈니스 로직 함수들을 구현하세요:
   - `add_stamp_record(score: int, word_count: int, note: str) -> tuple[bool, str]`: 현재 스탬프가 10 미만일 때 1 증가시키고, 히스토리 배열 최상단에 날짜(ISO)와 세부 시험 정보를 기록한 뒤 저장.
   - `undo_last_stamp() -> tuple[bool, str]`: 현재 스탬프가 1 이상일 때 1을 차감하고, 가장 최근 추가된 히스토리 1건을 롤백(제거)한 뒤 저장.
   - `reset_cycle() -> bool`: 10개 완주 후 선물을 수령했을 때, 기존 히스토리는 완벽히 보존한 채 `current_stamps`만 0으로 초기화.
5. 위시리스트 관리 함수들을 구현하세요:
   - `add_wishlist_item(title: str, image_url: str, memo: str) -> bool`: 고유 ID(`wish-` 접두사 + UUID)를 부여하여 리스트에 추가.
   - `delete_wishlist_item(item_id: str) -> bool`: 지정된 ID 항목 삭제.
   - `redeem_reward(item_id: str) -> bool`: 대상 선물의 `is_redeemed` 속성을 True로 갱신.
6. 모든 함수에는 엄격한 타입 힌트(Type Hints), 구체적인 예외 처리(try-except block), 그리고 명확한 한국어 Docstring을 작성해주세요.

### Step 2: 메인 화면 레이아웃 및 10칸 칭찬 도장판 컨트롤러 구현 (`app.py`)

당신은 Streamlit 전문 프론트엔드/풀스택 엔지니어입니다.
앞서 구현한 `utils/storage.py` 모듈과 연동하여 앱의 진입점인 `app.py`의 핵심 UI와 10칸 칭찬 도장판 레이아웃을 작성해야 합니다.

아래 요구사항을 반영하여 코드를 작성해주세요:
1. `st.set_page_config`를 사용하여 페이지 타이틀을 "Minty Stamp - 100점 도장판", 아이콘을 "🌟", 레이아웃을 "wide"로 선언하세요.
2. 애플리케이션 시작 시 세션 상태(`st.session_state['app_data']`)를 확인하고, 없으면 `storage.load_data()`를 호출하여 초기화하세요.
3. 상단 헤더 영역 구현:
   - "예원이의 매일매일 영어 단어 100점 도장판 🌟" 메인 타이틀을 렌더링하세요.
   - 아동이 직접 수정 가능한 '오늘의 각오' 텍스트 인풋과 저장 버튼을 두고, 수정 시 `storage.save_data()`가 실행되도록 연결하세요.
   - 현재 누적 도장 수를 바탕으로 `st.progress(current_stamps / 10)` 게이지 바를 배치하고 "10개 중 N개 완료! N0%" 텍스트를 출력하세요.
4. 메인 영역 10칸 칭찬 도장판 시각화:
   - 2행 5열 구조를 만들기 위해 `st.columns(5)` 컨테이너 2세트를 구성하세요.
   - 1부터 10까지 루프를 돌며 각 슬롯을 렌더링하세요.
   - 획득한 번호(i <= current_stamps): 큰 별 이모지('🌟'), "100점 참 잘했어요!", 획득 일자를 표시하는 카드 박스 렌더링.
   - 미획득 번호(i > current_stamps): 흐릿한 테두리와 슬롯 번호('⭐ i번째 별')만 표기되는 대기 카드 렌더링.
   - 10번째 슬롯은 최종 보상 슬롯이므로 왕관 이모지('👑')와 특별 강조 문구를 넣으세요.
5. 사이드바 '학부모 확인 패널' 구현:
   - `st.sidebar.checkbox("부모님 확인 모드")` 잠금 장치를 구현하세요.
   - 잠금 해제 시 단어 시험 점수, 외운 단어 수, 부모 칭찬 메모를 입력하는 폼과 "도장 쾅! 찍기 (100점 인정)" 버튼을 만드세요.
   - 클릭 시 `storage.add_stamp_record()`를 호출하고 성공 시 `st.rerun()`으로 화면을 즉시 갱신하세요.
   - 잘못 찍었을 때를 대비한 "실수 취소 (마지막 도장 1개 회수)" 버튼을 배치하고 `storage.undo_last_stamp()`와 연동하세요.

### Step 3: 선물 위시리스트 갤러리, 성공 일기 탭 및 피날레 로직 확장

당신은 아동용 서비스 UX 설계에 특화된 파이썬 프론트엔드 개발자입니다.
`app.py`를 고도화하여 세 개의 메인 탭(`st.tabs(["🌟 칭찬 도장판", "🎁 선물 위시리스트", "📖 100점 성공 일기"])`) 체계로 분리하고, 위시리스트 CRUD와 피날레 보상 선택 로직을 완성해주세요.

아래 요구사항을 충족하도록 코드를 작성해주세요:
1. **첫 번째 탭 (칭찬 도장판)**: Step 2에서 작성한 10칸 도장판과 각오 입력기를 그대로 배치합니다.
2. **두 번째 탭 (선물 위시리스트)**:
   - 상단에 `st.expander("✨ 새로운 선물 후보 등록하기")`를 만들고 선물 이름(필수), 이미지 웹 URL, 부모님과의 약속 메모를 입력받아 `storage.add_wishlist_item()`으로 저장하는 폼을 구현하세요.
   - 등록된 선물 리스트를 `st.columns(3)` 반응형 카드 그리드로 렌더링하세요. 각 카드에는 `st.image`로 사진을 출력하고(이미지 로드 실패 시 디폴트 플레이스홀더 사용), 제목, 메모, 삭제 버튼을 배치하세요.
   - **피날레 연동**: 만약 `current_stamps >= 10` 상태라면 각 선물 카드 하단에 "🎉 이 선물 받기!" 선택 버튼을 활성화하세요. 버튼 클릭 시 `storage.redeem_reward()`를 호출하여 수령 완료 뱃지를 달아주고 축하 성공 메시지를 띄우세요. 10개 미만일 때는 "아직 도장을 모으는 중이에요 (N/10)" 비활성 문구를 보여주세요.
3. **세 번째 탭 (100점 성공 일기)**:
   - 상단에 `st.metric` 3개를 나란히 배치하여 '총 100점 달성 횟수', '총 암기 단어 수', '현재 도장 달성률(%)'을 표시하세요.
   - 하단에는 `data['history']` 리스트를 역순으로 순회하며, 날짜, 회차 뱃지, 외운 단어 수, 칭찬 코멘트가 담긴 타임라인 카드를 정갈하게 출력하세요.
4. 사이드바 패널에 10개 도장을 모두 모아 선물을 수령한 후 다음 도전을 시작할 수 있는 "새로운 10개 도전 시작하기 (판 비우기)" 버튼을 추가하고, 클릭 시 확인 팝업 후 `storage.reset_cycle()`이 실행되도록 구현하세요.

### Step 4: 파스텔 민트 커스텀 CSS 테마, Confetti 및 사운드 효과 통합 (`utils/effects.py`)

당신은 인터랙티브 애니메이션 및 프론트엔드 스타일링을 담당하는 시니어 UI 엔지니어입니다.
8세 여아의 감성에 맞춘 파스텔 민트 테마와 다감각 피드백 모듈 `utils/effects.py`를 제작하고 전체 애플리케이션에 결합해주세요.

아래 세부 요구사항을 구현해주세요:
1. `utils/effects.py` 모듈 신규 구현:
   - `inject_custom_css()` 함수: 구글 웹폰트('Jua')를 임포트하고, 배경색(`#F7FCFB`), 카드 컨테이너 배경(`#DFF6F5`), 둥근 테두리(`border-radius: 20px`), 버튼 그라데이션(`#5CE1E6` -> `#70D6BC`), 호버 바운스 효과를 지정하는 완성도 높은 CSS를 `st.markdown`으로 주입하세요.
   - `trigger_confetti(mode='normal')` 함수: `[https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js](https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js)` 스크립트를 로드하고 `st.components.v1.html`을 통해 실행하는 코드를 작성하세요. 일반 스탬프 적립 시 양옆에서 가볍게 터지고, 10점 완주('grand') 모드 시 5초간 화면 전체에 쏟아지는 자바스크립트를 작성하세요.
   - `play_sound(sound_key)` 함수: `assets/sounds/{sound_key}.mp3` 파일을 바이너리로 읽어 Base64 인코딩한 뒤, `<audio autoplay style="display:none"><source src="data:audio/mp3;base64,..."></audio>` 태그를 `st.markdown(..., unsafe_allow_html=True)`로 렌더링하는 자동 재생 헬퍼를 작성하세요.
2. `app.py`와의 최종 결합:
   - 앱 렌더링 최상단에서 `effects.inject_custom_css()`를 호출하여 전역 스타일을 적용하세요.
   - 부모가 사이드바에서 100점 도장을 찍어 성공할 때마다 `trigger_confetti('normal')`와 `play_sound('stamp')`가 트리거되도록 연결하세요.
   - 누적 스탬프가 10개에 도달하는 순간 `trigger_confetti('grand')`와 `play_sound('celebration')`를 실행하고, 대형 축하 다이얼로그(`st.dialog` 또는 모달)를 띄워 "10개의 별을 모두 모았어요! 선물 탭에서 원하는 선물을 선택하세요!"를 알리세요.
3. 프로젝트 루트에 설치해야 할 패키지 목록을 담은 `requirements.txt` 내용(streamlit 최신 버전 등)과 터미널 실행 명령어(`streamlit run app.py`)를 안내해주세요.