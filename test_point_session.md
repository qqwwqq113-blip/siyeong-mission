# Minty Stamp 구현 작업 분할 계획

본 문서는 [test_point.md](test_point.md)에 명시된 'Minty Stamp' 구현 명세를 기준으로, 한 번에 많은 작업을 진행하지 않고 세부 구간별로 나누어 진행하기 위한 작업 계획서이다. 각 단계는 구현 책임, 필요한 파일, 핵심 기능, 검증 포인트를 포함하여, 문제가 발생했을 때 원인 파악과 수정 범위를 최소화할 수 있도록 구성하였다.

---

## 1. 작업 원칙

1. 기능 단위로 나누어 구현한다.
   - 데이터 저장/로딩 → 메인 화면 → 보상 시스템 → 시각 효과 순서로 진행한다.
2. 각 단계마다 최소 검증을 수행한다.
   - 기능이 동작하는지 로컬 실행으로 확인한다.
3. 의존성은 가장 낮은 계층부터 완성한다.
   - 스토리지 로직을 먼저 만든 뒤 UI와 연동한다.
4. 세션 상태와 파일 저장 로직을 분리한다.
   - UI는 데이터 상태를 읽고 쓰는 계층에 의존하도록 설계한다.
5. 8세 아동용 UX를 유지한다.
   - 기능 개발과 함께 버튼 크기, 색상, 문구 명확성, 피드백 메시지를 함께 검토한다.

---

## 2. 전체 구현 흐름

1. 프로젝트 환경 설정
2. 기본 디렉터리 및 파일 구조 구성
3. 데이터 스키마와 JSON 저장 모듈 구현
4. 앱 메인 화면 기본 구조 구성
5. 10칸 도장판 UI 및 상호작용 구현
6. 위시리스트 CRUD 기능 구현
7. 성공 일기/타임라인 화면 구현
8. 부모 모드/관리 기능 구현
9. CSS, confetti, 음향 효과 통합
10. 최종 통합 검증 및 리팩터링

---

## 3. 세부 작업 구간

## 구간 1. 프로젝트 초기 설정 및 구조 구성

### 목표
- Streamlit 기반 프로젝트 구조를 준비한다.
- 앱 실행 전 기본 환경과 파일 구조를 안정적으로 만든다.

### 진행 항목
- 루트 디렉터리 생성: minty_stamp/
- 필수 파일 생성:
  - app.py
  - requirements.txt
  - .streamlit/config.toml
  - data/ 폴더
  - assets/css/style.css
  - assets/sounds/ 폴더
  - utils/__init__.py
  - utils/storage.py
  - utils/effects.py
- Python 패키지 의존성 정의
  - streamlit
  - json 관련 표준 라이브러리 사용
  - 필요 시 Pillow, requests 등 선택적 추가 검토

### 산출물
- 실행 가능한 기본 폴더 구조
- requirements.txt
- app.py 진입점 기본 skeleton

### 검증 포인트
- Python 환경에서 app.py를 import 가능한지 확인
- app.py 실행 시 Streamlit 서버가 정상 기동되는지 확인
- 파일 경로 문제 없이 data/ 폴더 생성되는지 확인

### 주의사항
- 구조를 먼저 잡고 나서 기능을 추가해야, 나중에 파일 재배치가 줄어든다.
- 로컬 파일 경로는 절대 경로보다 애플리케이션 루트 기준 경로 계산 방식을 우선한다.

---

## 구간 2. 데이터 저장 계층 구현

### 목표
- 앱 데이터가 파일 기반으로 안전하게 저장되고 읽혀지도록 만든다.
- 추후 UI와 기능 로직이 모두 이 모듈에 의존하게 설계한다.

### 구현 파일
- utils/storage.py

### 세부 작업
1. 기본 스키마 정의
   - profile
   - wishlist
   - history
   - target_stamps = 10
   - current_stamps 기본값 0
2. 파일 경로 함수 구현
   - 프로젝트 루트 기준 data/app_data.json 경로 계산
   - 디렉터리 없을 때 자동 생성
3. 초기 JSON 생성 로직 구현
   - 파일이 없으면 표준 구조 생성
4. load_data() 구현
   - 파일 존재 여부 체크
   - JSON 역직렬화
   - 예외 처리 및 초기 스키마 복구
5. save_data() 구현
   - 안전한 원자적 저장
   - app_data.json.tmp 작성 후 os.replace 사용
   - ensure_ascii=False 및 indent=2 적용
6. add_stamp_record() 구현
   - 현재 도장이 10 미만인지 검사
   - 히스토리객체 생성
   - 날짜, 점수, 단어 수, 코멘트, 스탬프 상태 반영
   - 저장 후 성공/실패 메시지 반환
7. undo_last_stamp() 구현
   - 현재 도장 1 이상 확인
   - 가장 최근 히스토리 제거
   - 저장 후 되돌림 반영
8. reset_cycle() 구현
   - current_stamps만 0으로 초기화
   - history는 유지
9. 위시리스트 기능 구현
   - add_wishlist_item()
   - delete_wishlist_item()
   - redeem_reward()

### 검증 포인트
- 파일이 없을 때 자동 생성됨
- JSON 파일 내용이 제대로 저장됨
- 한글이 깨지지 않고 유지됨
- 도장 적립/취소가 current_stamps와 history를 함께 변화시키는지 확인
- 원자적 저장이 예외 상황에서도 데이터 손상 없이 동작하는지 확인

### 작업 분리 이유
- 이 단계는 이후 UI 구현의 기반이 되므로 가장 먼저 안정적으로 구현해야 한다.
- 문제 발생 시 UI 코드가 아니라 데이터 로직 문제인지 빠르게 분리 가능하다.

---

## 구간 3. 앱 진입점 및 화면 기본 구조 구성

### 목표
- Streamlit 앱의 기본 화면 골격을 만든다.
- 실질적인 기능을 붙이기 전 페이지 구조와 상태 초기화를 준비한다.

### 구현 파일
- app.py

### 세부 작업
1. st.set_page_config() 구성
   - title: Minty Stamp - 100점 도장판
   - icon: 🌟
   - layout: wide
2. session state 초기화
   - app_data 로딩
   - 데이터가 없으면 storage.load_data() 호출
3. 전역 CSS 주입
   - effects.inject_custom_css() 실행
4. 기본 탭 구조 준비
   - 3개 메인 탭 생성
   - 각각의 뷰 함수 연결 준비
5. 기본 데이터 표시용 helper 구성
   - current_stamps 계산
   - progress rate 계산
   - child_name 표시

### 검증 포인트
- 앱 실행 시 title과 layout이 정상 반영됨
- 세션 상태가 초기화됨
- CSS가 정상 주입됨
- 탭이 3개로 나뉘어 보임

### 작업 분리 이유
- UI 코드가 너무 빨리 커지면 구조 파악이 어려워지므로, 화면 골격을 먼저 완성해야 한다.

---

## 구간 4. 상단 헤더 및 목표 진행 UI 구현

### 목표
- 아동이 화면을 보자마자 상태를 이해할 수 있는 상단 영역을 만든다.

### 구현 위치
- app.py
- render_header() 함수

### 세부 작업
1. 메인 타이틀 구성
   - 예원이의 매일매일 영어 단어 100점 도장판 🌟
2. 오늘의 각오 한마디 입력 UI
   - text_input 또는 text_area 사용
   - 저장 버튼으로 profile.daily_memo 업데이트
3. 진행률 게이지 구성
   - current_stamps / 10
   - st.progress 사용
4. 상태 문구 구성
   - 현재 10개 중 N개 달성! N% 완료!
5. 저장 후 화면 갱신 처리
   - 수정 후 st.rerun() 또는 상태 반영

### 검증 포인트
- 저장 시 상단 배너의 메시지가 바로 반영됨
- progress bar 값이 정확히 계산됨
- 각오 문구가 데이터 파일에 반영됨

### 주의사항
- 문구는 아동이 이해할 수 있게 짧고 직관적으로 구성한다.
- 너무 긴 문장은 화면을 깨지 않도록 줄바꿈과 여백을 고려한다.

---

## 구간 5. 10칸 칭찬 도장판 구현

### 목표
- 핵심 기능인 도장판 UI를 완성한다.
- 100점 적립 시 시각적으로 보상 느낌을 살린다.

### 구현 위치
- app.py
- render_stamp_board() 함수

### 세부 작업
1. 2행 5열 구조 생성
   - st.columns(5) 2세트 사용
2. 슬롯 렌더링 루프 구현
   - 1~10까지 순회
3. 획득 상태 렌더링
   - 별 아이콘, 날짜, 히스토리 문구
   - 황금색 배경과 강조
4. 미획득 상태 렌더링
   - 점선 테두리, 연한 배경, 번호 표시
5. 10번째 슬롯 강조
   - 왕관 아이콘, 펄스 효과 스타일 적용
6. 세부 문구 정리
   - "참 잘했어요!"
   - "마지막 별을 향해 가고 있어요!"

### 검증 포인트
- current_stamps 수에 맞춰 획득/미획득 슬롯이 정확히 표시됨
- 10개 도장 도달 시 마지막 슬롯 강조가 적용됨
- 도장 수와 문구가 일치하는지 확인

### 작업 분리 이유
- 도장판은 핵심 사용자 경험을 좌우하므로, UI를 따로 분리해 구현하는 것이 가장 안전하다.

---

## 구간 6. 선물 위시리스트 CRUD 기능 구현

### 목표
- 보상 제도와 연동되는 선물 등록 및 선택 기능을 만든다.

### 구현 위치
- app.py
- render_wishlist_view() 함수
- utils/storage.py

### 세부 작업
1. 신규 선물 등록 폼 구현
   - 선물 이름 입력
   - 이미지 URL 입력
   - 메모 입력
   - 저장 버튼
2. add_wishlist_item() 연결
   - 필수값 검증
   - 고유 ID 부여
   - JSON 저장
3. 카드형 그리드 렌더링
   - st.columns(3)
   - 각 카드에 제목, 메모, 이미지 표시
4. 삭제 버튼 구현
   - delete_wishlist_item() 연결
5. 보상 선택 로직
   - current_stamps >= 10일 때 선택 버튼 활성화
   - 클릭 시 redeem_reward() 호출
6. 미완료 상태 안내 문구
   - 아직 도장을 모으는 중이에요 (N/10)

### 검증 포인트
- 새 선물 등록 후 리스트에 반영됨
- 삭제 시 정확히 제거됨
- 도장이 10개 이상일 때만 선택 버튼이 활성화됨
- is_redeemed가 true로 저장됨

### 작업 분리 이유
- 위시리스트는 보상 모델과 직결되므로, 도장판이 안정화된 뒤 별도 구현하는 것이 낫다.

---

## 구간 7. 성공 일기 및 타임라인 구현

### 목표
- 학습 성취를 축적한 기록을 사용자에게 시각적으로 보여준다.

### 구현 위치
- app.py
- render_history_view() 함수

### 세부 작업
1. 총 100점 달성 횟수 메트릭 계산
2. 총 암기 단어 수 합산
3. 도장 달성률 계산
4. history 리스트 역순 정렬
5. 카드형 타임라인 렌더링
   - 날짜
   - 회차/점수
   - 단어 수
   - 메모
6. 빈 기록 처리
   - 아직 기록이 없을 때 안내 메시지 표시

### 검증 포인트
- history 데이터를 정확히 집계한다.
- 로컬 데이터 파일의 기록이 화면에 반영된다.
- 빈 데이터 상태에서도 앱이 깨지지 않는다.

### 작업 분리 이유
- 기록 화면은 보상 및 도장 로직이 완성된 뒤에 구현하는 것이 가장 안정적이다.

---

## 구간 8. 학부모 관리 패널 구현

### 목표
- 시험 인정, 실수 취소, 10개 도전 초기화를 부모 관리자 기능으로 제한한다.

### 구현 위치
- app.py
- render_parent_sidebar() 함수

### 세부 작업
1. 부모 확인 모드 체크박스 구현
2. 100점 적립 입력 폼 구현
   - 날짜
   - 단어 수
   - 칭찬 메모
3. add_stamp_record() 연결
4. 실수 취소 버튼 구현
   - undo_last_stamp()와 연결
5. 새 도전 시작 버튼 구현
   - reset_cycle() 호출
   - 확인 대화상자 처리
6. 접근 권한 로직
   - 부모 모드가 켜졌을 때만 기능 노출

### 검증 포인트
- 부모 모드가 꺼져 있으면 버튼이 비활성화됨
- 부모 모드가 켜지면 입력 폼과 버튼이 실제로 활성화됨
- 실수 취소 후 도장 수와 history가 일관되게 줄어든다.

### 작업 분리 이유
- 학부모 기능은 보안/검증 측면이 있으므로 안정적인 데이터 로직이 끝난 뒤 구현해야 한다.

---

## 구간 9. CSS/애니메이션/사운드 효과 통합

### 목표
- 8세 아동 대상 UX를 감각적으로 완성한다.

### 구현 위치
- utils/effects.py
- app.py
- assets/css/style.css

### 세부 작업
1. CSS 주입 구현
   - Jua 웹폰트 로딩
   - 파스텔 민트 테마 적용
   - 둥근 카드, 둥근 버튼, 배경색, 그라데이션
   - hover 시 bounce 효과
2. confetti 구현
   - normal mode: 양옆에서 터짐
   - grand mode: 전체 화면 폭죽 효과
   - js 라이브러리를 렌더링하는 방식 적용
3. 오디오 효과 구현
   - stamp.mp3
   - celebration.mp3
   - Base64 인코딩 기반 자동 재생
4. app.py와 연결
   - 도장 적립 성공 시 confetti + sound 실행
   - 10개 달성 시 grand 효과 + 축하 다이얼로그
5. 축하 다이얼로그 구성
   - 10개의 별을 모두 모았어요! 선물 탭에서 원하는 선물을 선택하세요!

### 검증 포인트
- CSS 적용이 페이지 전역에 반영됨
- confetti가 실행되는지 브라우저에서 확인
- 오디오가 자동 재생 가능한지 확인
- 10개 도장 달성 시 축하 메시지가 표시됨

### 작업 분리 이유
- 애니메이션 효과는 마지막 통합 단계에서 연결해야, 기능 흐름을 방해하지 않고 실시간 디버깅이 가능하다.

---

## 구간 10. 최종 통합, 정리, 검증

### 목표
- 전체 앱이 일관되게 동작하도록 통합하고 버그를 정리한다.

### 세부 작업
1. app.py 전체 점검
   - 함수 경계와 import 정리
   - 중복 코드 축소
2. 기능 흐름 확인
   - 도장 적립 → 스토리지 저장 → 화면 갱신 → 효과 재생
   - 선물 등록 → 보상 선택 → is_redeemed 반영
   - 10개 완료 → 승리 메시지 → reset_cycle 가능
3. 예외 처리 확인
   - JSON 깨짐
   - 이미지 URL 실패
   - 파일 없음
   - 빈 리스트
4. UX 점검
   - 버튼 크기 충분한지
   - 텍스트 이해 가능 여부
   - 색상 대비 확인
5. requirements.txt 정리
6. 실행 문서 정리
   - streamlit run app.py

### 검증 포인트
- 전체 앱이 정상 실행됨
- 주요 기능 플로우가 모두 동작함
- 에러 없이 종료/재시작 가능함
- 파일 저장이 유지됨

### 작업 분리 이유
- 마지막에 전체 검증을 하면, 어떤 단계에서 문제가 발생했는지 원인 추적이 쉬워진다.

---

## 4. 권장 구현 순서 요약

1. 초기 구조와 파일 만들기
2. 데이터 저장/JSON 엔진 구현
3. 기본 Streamlit 화면 구조 구성
4. 상단 헤더/도장판 UI 구현
5. 위시리스트 CRUD 구현
6. 성공 일기 화면 구현
7. 학부모 관리 기능 구현
8. CSS, confetti, 사운드 효과 연동
9. 전체 통합 검증

이 순서를 따르면, 한 번에 많은 코드 작업을 진행할 때 생기는 문제를 줄이고, 각 단계별 결과물을 바로 검증할 수 있다.

---

## 5. 구현 체크리스트

### 필수 완료 조건
- [ ] app.py 실행 가능
- [ ] app_data.json 저장/로드 동작
- [ ] 도장 적립/취소 동작
- [ ] 리스트 추가/삭제/보상 선택 동작
- [ ] 10개 달성 시 축하 효과 동작
- [ ] 최종 래퍼 구조와 다중 탭 UI 동작
- [ ] 로컬 환경에서 전체 기능 검증 완료

### 선택 완료 조건
- [ ] 파스텔 CSS 세련화
- [ ] confetti 애니메이션 튜닝
- [ ] 오디오 효과 품질 개선
- [ ] 추가적인 사용자 친화 문구 정리

---

## 6. 다음 작업 시작 지점

다음 실제 구현은 구간 1부터 순차적으로 진행한다. 가장 먼저 해야 할 일은 아래 3가지이다.

1. minty_stamp/ 루트 구조를 만든다.
2. utils/storage.py의 기본 스키마와 저장 로직을 구현한다.
3. app.py의 진입점과 기본 탭 구조를 만든다.

이 세 단계가 끝나면 이후 기능 구현의 기준이 명확해져서, 각 단계별 문제를 빠르게 분리하고 수정할 수 있다.
